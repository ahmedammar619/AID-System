const translations = {
    "You are here": {
        English: "You are here",
        Arabic: "أنت هنا",
        Farsi: "شما اینجا هستید",
        Spanish: "Estás aquí",
        Urdu: "آپ یہاں ہیں",
        Myanmar: "သင်ဒီမှာရှိသည်",
        Pashto: "تاسو دلته یاست"
    },
    "Collect Location:": {
        English: "Collect Location:",
        Arabic: "موقع التجميع:",
        Farsi: "محل جمع آوری:",
        Spanish: "Lugar de recogida:",
        Urdu: "جمع کرنے کا مقام:",
        Myanmar: "စုဆောင်းရန်နေရာ:",
        Pashto: "د راټولولو ځای:"
    },
    "Total Recipients:": {
        English: "Total Recipients:",
        Arabic: "إجمالي المستلمين:",
        Farsi: "مجموع دریافت کنندگان:",
        Spanish: "Total de destinatarios:",
        Urdu: "کل وصول کنندگان:",
        Myanmar: "လက်ခံသူစုစုပေါင်း:",
        Pashto: "د ترلاسه کونکو مجموعه:"
    },
    "Are you sure you want to deselect": {
        English: "Are you sure you want to deselect",
        Arabic: "هل أنت متأكد أنك تريد إلغاء تحديد",
        Farsi: "آیا مطمئن هستید که می خواهید لغو انتخاب کنید",
        Spanish: "¿Estás seguro de que deseas deseleccionar a",
        Urdu: "کیا آپ واقعی غیر منتخب کرنا چاہتے ہیں",
        Myanmar: "သင် မရွေးချယ်လိုသည်မှာ သေချာပါသလား",
        Pashto: "آیا تاسو ډاډه یاست چې غواړئ ناټاکل کړئ"
    },
    "Are you sure you want to select": {
        English: "Are you sure you want to select",
        Arabic: "هل أنت متأكد أنك تريد تحديد",
        Farsi: "آیا مطمئن هستید که می خواهید انتخاب کنید",
        Spanish: "¿Estás seguro de que deseas seleccionar a",
        Urdu: "کیا آپ واقعی منتخب کرنا چاہتے ہیں",
        Myanmar: "သင် ရွေးချယ်လိုသည်မှာ သေချာပါသလား",
        Pashto: "آیا تاسو ډاډه یاست چې غواړئ وټاکئ"
    },
    "Starting Time": {
        English: "Starting Time",
        Arabic: "وقت البدء",
        Farsi: "زمان شروع",
        Spanish: "Hora de inicio",
        Urdu: "شروع کا وقت",
        Myanmar: "စတင်ချိန်",
        Pashto: "د پیل وخت"
    },
    "Phone": {
        English: "Phone",
        Arabic: "الهاتف",
        Farsi: "تلفن",
        Spanish: "Teléfono",
        Urdu: "فون",
        Myanmar: "ဖုန်း",
        Pashto: "تلیفون"
    },
    "Address": {
        English: "Address",
        Arabic: "العنوان",
        Farsi: "آدرس",
        Spanish: "Dirección",
        Urdu: "پتہ",
        Myanmar: "လိပ်စာ",
        Pashto: "پته"
    },
    "Maps": {
        English: "Maps",
        Arabic: "الخرائط",
        Farsi: "نقشه ها",
        Spanish: "Mapas",
        Urdu: "نقشے",
        Myanmar: "မြေပုံများ",
        Pashto: "نقشې"
    },
    "Actions": {
        English: "Actions",
        Arabic: "الإجراءات",
        Farsi: "اقدامات",
        Spanish: "Acciones",
        Urdu: "اعمال",
        Myanmar: "လုပ်ဆောင်ချက်များ",
        Pashto: "کړنې"
    },
    "Recipient": {
        English: "Recipient",
        Arabic: "المستلم",
        Farsi: "دریافت کننده",
        Spanish: "Destinatario",
        Urdu: "وصول کنندہ",
        Myanmar: "လက်ခံသူ",
        Pashto: "ترلاسه کوونکی"
    },
    "Items": {
        English: "Items",
        Arabic: "العناصر",
        Farsi: "موارد",
        Spanish: "Artículos",
        Urdu: "اشیاء",
        Myanmar: "ပစ္စည်းများ",
        Pashto: "توکي"
    },
    "Select": {
        English: "Select",
        Arabic: "تحديد",
        Farsi: "انتخاب",
        Spanish: "Seleccionar",
        Urdu: "منتخب کریں",
        Myanmar: "ရွေးချယ်ပါ",
        Pashto: "ټاکل"
    },
    "Deselect All": {
        English: "Deselect All",
        Arabic: "إلغاء تحديد الكل",
        Farsi: "لغو انتخاب همه",
        Spanish: "Deseleccionar todo",
        Urdu: "سب کو غیر منتخب کریں",
        Myanmar: "အားလုံးကို မရွေးချယ်ပါ",
        Pashto: "ټول ناټاکل کړئ"
    },
    "Are you sure you want to confirm these recipients?": {
        English: "Are you sure you want to confirm these recipients?",
        Arabic: "هل أنت متأكد أنك تريد تأكيد هؤلاء المستلمين؟",
        Farsi: "آیا مطمئن هستید که می خواهید این دریافت کنندگان را تأیید کنید؟",
        Spanish: "¿Estás seguro de que deseas confirmar a estos destinatarios?",
        Urdu: "کیا آپ واقعی ان وصول کنندگان کی تصدیق کرنا چاہتے ہیں؟",
        Myanmar: "ဤလက်ခံသူများကို အတည်ပြုလိုသည်မှာ သေချာပါသလား။",
        Pashto: "آیا تاسو ډاډه یاست چې غواړئ دا ترلاسه کونکي تایید کړئ؟"
    },
    "Please Don't forget to confirm the recipients after selecting all of them from the bottom of the page": {
        English: "Please Don't forget to confirm the recipients after selecting all of them from the bottom of the page",
        Arabic: "من فضلك لا تنس تأكيد المستلمين بعد اختيارهم جميعًا من أسفل الصفحة",
        Farsi: "لطفاً پس از انتخاب همه دریافت کنندگان از پایین صفحه، تأیید آنها را فراموش نکنید",
        Spanish: "Por favor, no olvide confirmar a los destinatarios después de seleccionarlos todos desde la parte inferior de la página",
        Urdu: "براہ کرم صفحے کے نیچے سے سب کو منتخب کرنے کے بعد وصول کنندگان کی تصدیق کرنا مت بھولیں",
        Myanmar: "စာမျက်နှာ၏အောက်ခြေမှ အားလုံးကို ရွေးချယ်ပြီးနောက် လက်ခံသူများကို အတည်ပြုရန် မမေ့ပါနှင့်",
        Pashto: "مهرباني وکړئ د پاڼې څخه ټول ټاکل کړئ وروسته د ترلاسه کونکو تایید مه هیروئ"
    },
    "Are you sure you want to delete all recipients?": {
        English: "Are you sure you want to delete all recipients?",
        Arabic: "هل أنت متأكد أنك تريد حذف جميع المستلمين؟",
        Farsi: "آیا مطمئن هستید که می خواهید همه دریافت کنندگان را حذف کنید؟",
        Spanish: "¿Estás seguro de que deseas eliminar a todos los destinatarios?",
        Urdu: "کیا آپ واقعی تمام وصول کنندگان کو حذف کرنا چاہتے ہیں؟",
        Myanmar: "သင် လက်ခံသူအားလုံးကို ဖျက်လိုသည်မှာ သေချာပါသလား။",
        Pashto: "آیا تاسو ډاډه یاست چې غواړئ ټول ترلاسه کونکي حذف کړئ؟"
    },
    "If you arrived at the pickup location and picked up the": {
        English: "If you arrived at the pickup location and picked up the",
        Arabic: "إذا وصلت إلى موقع الاستلام والتقطت",
        Farsi: "اگر به محل جمع آوری رسیده اید و آن را برداشته اید",
        Spanish: "Si llegaste al lugar de recogida y recogiste el",
        Urdu: "اگر آپ پک اپ مقام پر پہنچ گئے ہیں اور اسے اٹھا لیا ہے",
        Myanmar: "သင်သည် ကောက်ယူရန်နေရာသို့ ရောက်ရှိပြီး ကောက်ယူပါက",
        Pashto: "که تاسو د راټولولو ځای ته ورسېدئ او یې راټول کړئ"
    },
    ", please click the button below to confirm the pickup.": {
        English: ", please click the button below to confirm the pickup.",
        Arabic: "، يرجى النقر على الزر أدناه لتأكيد الاستلام.",
        Farsi: "، لطفاً برای تأیید جمع آوری، دکمه زیر را کلیک کنید.",
        Spanish: ", haga clic en el botón a continuación para confirmar la recogida.",
        Urdu: "، براہ کرم پک اپ کی تصدیق کرنے کے لیے نیچے دیے گئے بٹن پر کلک کریں۔",
        Myanmar: "၊ ကျေးဇူးပြု၍ ကောက်ယူမှုကို အတည်ပြုရန် အောက်ပါခလုတ်ကို နှိပ်ပါ။",
        Pashto: ", مهرباني وکړئ د راټولولو تایید لپاره لاندې تڼۍ کلیک وکړئ."
    },
    "Are you sure you want to confirm the pickup of the selected recipients?": {
        English: "Are you sure you want to confirm the pickup of the selected recipients?",
        Arabic: "هل أنت متأكد أنك تريد تأكيد استلام المستلمين المحددين؟",
        Farsi: "آیا مطمئن هستید که می خواهید جمع آوری دریافت کنندگان انتخاب شده را تأیید کنید؟",
        Spanish: "¿Estás seguro de que deseas confirmar la recogida de los destinatarios seleccionados?",
        Urdu: "کیا آپ واقعی منتخب کردہ وصول کنندگان کے پک اپ کی تصدیق کرنا چاہتے ہیں؟",
        Myanmar: "သင် ရွေးချယ်ထားသော လက်ခံသူများ၏ ကောက်ယူမှုကို အတည်ပြုလိုသည်မှာ သေချာပါသလား။",
        Pashto: "آیا تاسو ډاډه یاست چې غواړئ ټاکل شوي ترلاسه کونکو د راټولولو تایید وکړئ؟"
    },
    "Selected Recipients:": {
        English: "Selected Recipients:",
        Arabic: "المستلمون المحددون:",
        Farsi: "دریافت کنندگان انتخاب شده:",
        Spanish: "Destinatarios seleccionados:",
        Urdu: "منتخب کردہ وصول کنندگان:",
        Myanmar: "ရွေးချယ်ထားသော လက်ခံသူများ:",
        Pashto: "ټاکل شوي ترلاسه کونکي:"
    },
    "Total Required Boxes:": {
        English: "Total Required Boxes:",
        Arabic: "إجمالي الصناديق المطلوبة:",
        Farsi: "مجموع جعبه های مورد نیاز:",
        Spanish: "Total de cajas requeridas:",
        Urdu: "کل مطلوبہ ڈبے:",
        Myanmar: "လိုအပ်သော သေတ္တာစုစုပေါင်း:",
        Pashto: "د اړتیا وړ ټول بکسونه:"
    },
    "Complex Name": {
        English: "Complex Name",
        Arabic: "اسم المجمع",
        Farsi: "نام مجتمع",
        Spanish: "Nombre del complejo",
        Urdu: "کمپلیکس کا نام",
        Myanmar: "ကွန်ပလက်အမည်",
        Pashto: "د کمپلېکس نوم"
    },
    "Gate Code": {
        English: "Gate Code",
        Arabic: "رمز البوابة",
        Farsi: "کد دروازه",
        Spanish: "Código de la puerta",
        Urdu: "گیٹ کوڈ",
        Myanmar: "ဂိတ်ကုဒ်",
        Pashto: "د دروازې کوډ"
    },
    "Please confirm the delivery after dropping off each box to it's recipient.": {
        English: "Please confirm the delivery after dropping off each box to it's recipient.",
        Arabic: "يرجى تأكيد التسليم بعد تسليم كل صندوق إلى مستلمه.",
        Farsi: "لطفاً پس از تحویل هر جعبه به دریافت کننده آن، تحویل را تأیید کنید.",
        Spanish: "Por favor, confirme la entrega después de dejar cada caja en su destinatario.",
        Urdu: "براہ کرم ہر ڈبے کو اس کے وصول کنندہ تک پہنچانے کے بعد ڈیلیوری کی تصدیق کریں۔",
        Myanmar: "ကျေးဇူးပြု၍ သေတ္တာတစ်ခုစီကို လက်ခံသူထံ ပို့ဆောင်ပြီးနောက် ပို့ဆောင်မှုကို အတည်ပြုပါ။",
        Pashto: "مهرباني وکړئ د هر بکس د ترلاسه کوونکي ته رسولو وروسته د تحویل تایید وکړئ."
    },
    "Are you sure you want to confirm this recipient": {
        English: "Are you sure you want to confirm this recipient",
        Arabic: "هل أنت متأكد أنك تريد تأكيد هذا المستلم",
        Farsi: "آیا مطمئن هستید که می خواهید این دریافت کننده را تأیید کنید",
        Spanish: "¿Estás seguro de que deseas confirmar a este destinatario",
        Urdu: "کیا آپ واقعی اس وصول کنندہ کی تصدیق کرنا چاہتے ہیں",
        Myanmar: "သင် ဤလက်ခံသူကို အတည်ပြုလိုသည်မှာ သေချာပါသလား",
        Pashto: "آیا تاسو ډاډه یاست چې غواړئ دا ترلاسه کوونکی تایید کړئ"
    },
    "Please Select recipients from the map": {
        English: "Please Select recipients from the map",
        Arabic: "يرجى تحديد المستلمين من الخريطة",
        Farsi: "لطفاً دریافت کنندگان را از نقشه انتخاب کنید",
        Spanish: "Por favor, seleccione a los destinatarios en el mapa",
        Urdu: "براہ کرم نقشے سے وصول کنندگان کو منتخب کریں",
        Myanmar: "မြေပုံမှ လက်ခံသူများကို ရွေးချယ်ပါ",
        Pashto: "مهرباني وکړئ د نقشې څخه ترلاسه کونکي وټاکئ"
    },
    "Open Full Route On Google Maps": {
        English: "Open Full Route On Google Maps",
        Arabic: "افتح المسار الكامل على خرائط Google",
        Farsi: "مسیر کامل را در Google Maps باز کنید",
        Spanish: "Abrir ruta completa en Google Maps",
        Urdu: "Google Maps پر مکمل راستہ کھولیں",
        Myanmar: "Google Maps တွင် လမ်းကြောင်းအပြည့်အစုံကို ဖွင့်ပါ",
        Pashto: "په Google Maps کې د بشپړ مسیر خلاص کړئ"
    },
    "You will be redirected to Google Maps to view the route of the selected recipients.": {
        English: "You will be redirected to Google Maps to view the route of the selected recipients.",
        Arabic: "سيتم إعادة توجيهك إلى خرائط Google لعرض مسار المستلمين المحددين.",
        Farsi: "شما به Google Maps هدایت خواهید شد تا مسیر دریافت کنندگان انتخاب شده را مشاهده کنید.",
        Spanish: "Serás redirigido a Google Maps para ver la ruta de los destinatarios seleccionados.",
        Urdu: "آپ کو منتخب کردہ وصول کنندگان کے راستے کو دیکھنے کے لیے Google Maps پر ری ڈائریکٹ کیا جائے گا۔",
        Myanmar: "ရွေးချယ်ထားသော လက်ခံသူများ၏ လမ်းကြောင်းကို ကြည့်ရှုရန် Google Maps သို့ ပြန်ညွှန်းပေးမည်။",
        Pashto: "تاسو به د ټاکل شوي ترلاسه کونکو د مسیر د لیدو لپاره Google Maps ته بیاځای کړئ."
    },
    "It will cointain a maximum of 10 recipient waypoints, who are the nearest recipients to you. So please confirm delivery then re-click the button to view the route.": {
        English: "It will contain a maximum of 10 recipient waypoints, who are the nearest recipients to you. So please confirm delivery then re-click the button to view the route.",
        Arabic: "سيحتوي على حد أقصى 10 نقاط طريق للمستلمين، وهم أقرب المستلمين إليك. لذا يرجى تأكيد التسليم ثم انقر فوق الزر مرة أخرى لعرض المسار.",
        Farsi: "این شامل حداکثر 10 نقطه راه برای دریافت کنندگان خواهد بود، که نزدیکترین دریافت کنندگان به شما هستند. بنابراین لطفاً تحویل را تأیید کنید و سپس دوباره روی دکمه کلیک کنید تا مسیر را مشاهده کنید.",
        Spanish: "Contendrá un máximo de 10 puntos de referencia de destinatarios, que son los destinatarios más cercanos a usted. Por lo tanto, confirme la entrega y luego vuelva a hacer clic en el botón para ver la ruta.",
        Urdu: "اس میں زیادہ سے زیادہ 10 وصول کنندہ وے پوائنٹس ہوں گے، جو آپ کے قریب ترین وصول کنندگان ہیں۔ لہذا براہ کرم ڈیلیوری کی تصدیق کریں اور پھر راستہ دیکھنے کے لیے بٹن پر دوبارہ کلک کریں۔",
        Myanmar: "၎င်းတွင် အများဆုံး လက်ခံသူ ၁၀ ဦး၏ လမ်းကြောင်းများ ပါဝင်မည်ဖြစ်ပြီး၊ ၎င်းတို့သည် သင့်အနီးဆုံး လက်ခံသူများ ဖြစ်သည်။ ကျေးဇူးပြု၍ ပို့ဆောင်မှုကို အတည်ပြုပြီး လမ်းကြောင်းကို ကြည့်ရှုရန် ခလုတ်ကို နှိပ်ပါ။",
        Pashto: "دا به د ۱۰ ترلاسه کونکو د لارې نقطې ولري، چې ستاسو ته نږدې ترلاسه کونکي دي. نو مهرباني وکړئ تحویل تایید کړئ او بیا د مسیر د لیدو لپاره تڼۍ بیا کلیک کړئ."
    },
    "Confirm Pickup": {
        English: "Confirm Pickup",
        Arabic: "تأكيد الاستلام",
        Farsi: "تأیید جمع آوری",
        Spanish: "Confirmar recogida",
        Urdu: "تصدیق کریں",
        Myanmar: "အတည်ပြုပါ",
        Pashto: "تایید کړئ"
    },
    "Confirm Delivery": {
        English: "Confirm Delivery",
        Arabic: "تأكيد التسليم",
        Farsi: "تأیید تحویل",
        Spanish: "Confirmar entrega",
        Urdu: "ڈیلیوری کی تصدیق کریں",
        Myanmar: "ပို့ဆောင်မှုအတည်ပြုပါ",
        Pashto: "د تحویل تایید کړئ"
    },
    "Confirm": {
        English: "Confirm",
        Arabic: "تأكيد",
        Farsi: "تأیید",
        Spanish: "Confirmar",
        Urdu: "تصدیق کریں",
        Myanmar: "အတည်ပြုပါ",
        Pashto: "تایید کړئ"
    },
    "Deselect": {
        English: "Deselect",
        Arabic: "إلغاء التحديد",
        Farsi: "لغو انتخاب",
        Spanish: "Deseleccionar",
        Urdu: "غیر منتخب کریں",
        Myanmar: "မရွေးချယ်ပါ",
        Pashto: "ناټاکل کړئ"
    },
    "Collect Locations:": {
        English: "Collect Locations:",
        Arabic: "مواقع التجميع:",
        Farsi: "محل های جمع آوری:",
        Spanish: "Lugares de recogida:",
        Urdu: "جمع کرنے کے مقامات:",
        Myanmar: "စုဆောင်းရန်နေရာများ:",
        Pashto: "د راټولولو ځایونه:"
    }
};

const currentLanguage = (localStorage.getItem('preferredLanguage'));
function translate(key) {
    return translations[key]?.[currentLanguage] || key; // Fallback to the key if translation is missing
}


// function setLanguage(lang) {
//     currentLanguage = lang;
//     updateTexts();
// }

// function translate(key) {
//     return translations[key]?.[currentLanguage] || key; // Fallback to the key if translation is missing
// }

// function updateTexts() {
//     // Example: Update tooltips, buttons, headers, etc.
//     document.querySelectorAll('[data-translate]').forEach(element => {
//         const key = element.getAttribute('data-translate');
//         element.innerText = translate(key);
//     });
// }


let userLocation = JSON.parse(sessionStorage.getItem('userLocation'));

document.addEventListener('DOMContentLoaded', function() {

// location of the user

    if (userLocation) {
        userLocation = userLocation.value;
        document.querySelector('#recipient-header').style.display = 'block';
    } 
    else{
        document.querySelector('.delivery-header-div').style.width = '350px';
        document.querySelector('.delivery-header-div').style.paddingBottom = '45px';
        document.querySelector('.delivery-header-div').className = 'login-div login-popup';
}

});

async function initMap(recipients, collectLocation) {
    // Set the zoom level of the map
    const centerLat = parseFloat(localStorage.getItem('mapCenterLat'));
    const centerLng = parseFloat(localStorage.getItem('mapCenterLng'));
    const zoom = parseInt(localStorage.getItem('mapZoom'));

    let map;
    if(zoom){
        map = L.map('map',{zoomControl: false}).setView([centerLat, centerLng], zoom);
    }else{
        map = L.map('map',{
            zoomControl: false // disable the default zoom control
        }).setView([userLocation.lat, userLocation.lng], 13); // Set the initial position and zoom level
    }

    // Custom zoom control
    L.control.zoom({
        position: 'bottomright'
    }).addTo(map);

    // Add a custom marker icon for user
    const userIcon = L.icon({
        iconUrl: 'https://raw.githubusercontent.com/pointhi/leaflet-color-markers/master/img/marker-icon-2x-red.png',
        shadowUrl: 'https://cdnjs.cloudflare.com/ajax/libs/leaflet/0.7.7/images/marker-shadow.png',
        iconSize: [25, 41],
        shadowSize: [41, 41],
        iconAnchor: [12, 41],
        shadowAnchor: [4, 62],
        popupAnchor: [1, -34]
    });

    // Load the map tiles from OpenStreetMap
    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
        maxZoom: 19,
        attribution: 'Map data © <a href="https://www.openstreetmap.org/">OpenStreetMap</a> contributors'
    }).addTo(map);

    // Add a marker for the user's location
    const userMarker = L.marker([userLocation.lat, userLocation.lng], {icon: userIcon}).addTo(map);
    userMarker.bindTooltip(`${translate('You are here')}`, { permanent: false, direction: "top" });

    if(collectLocation) {
        collectLocation.forEach(collectLocation => {
            const collectLocationIcon = L.divIcon({
                className: 'custom-icon',
                iconSize: [25, 41],
                html: `<svg height="50" width="50">
                        <circle cx="25" cy="25" r="20" stroke="black" stroke-width="3" fill="yellow" />
                       </svg>`
            });
            const collectLocationMarker = L.marker([collectLocation.lat, collectLocation.lng], {icon: collectLocationIcon}).addTo(map);
            collectLocationMarker.bindTooltip(`${translate('Collect Location:')} ${collectLocation.address}`, { permanent: false, direction: "top" });
        });
    }

    const recipientHeaderText = document.getElementById('recipient-header');
    recipientHeaderText.innerHTML = `<span style="color:rgb(0, 123, 255)">${translate('Total Recipients:')} ${recipients.length}</span>`;

    let minDistance = 10000000;
    let totalItems = 0;
    let selectedRecipients = [];
    let confirmedRecipients = [];
    let spawnedRecipientMarkers = [];

    // Create a Supercluster instance with fixed geographic distance
    const cluster = new Supercluster({
        radius: 40, // This will stay fixed but we will use fixed distance in miles for clustering
        maxZoom: 16,
        minZoom: 0
    });

    let recipientPoints = recipients.map(recipient => ({
        type: 'Feature',
        properties: {
            cluster: false,
            recipientId: recipient.id,
            items: recipient.items,
            address: recipient.address,
            distance: recipient.distance,
        },
        geometry: {
            type: 'Point',
            coordinates: [recipient.lng, recipient.lat],
        },
    }));

    cluster.load(recipientPoints);

    // Function to calculate distance in miles using the Haversine formula
    function haversineDistance(lat1, lon1, lat2, lon2) {
        const toRad = x => x * Math.PI / 180;
        const R = 3959; // Radius of Earth in miles
        const dLat = toRad(lat2 - lat1);
        const dLon = toRad(lon2 - lon1);
        const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
                  Math.cos(toRad(lat1)) * Math.cos(toRad(lat2)) *
                  Math.sin(dLon / 2) * Math.sin(dLon / 2);
        const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
        return R * c;
    }

    function updateClusters() {
        const bounds = map.getBounds();
        const bbox = [
            bounds.getWest(), bounds.getSouth(),
            bounds.getEast(), bounds.getNorth()
        ];

        const clusters = cluster.getClusters(bbox, 16); // Keep zoom level constant for clustering

        // Remove old markers
        spawnedRecipientMarkers.forEach(marker => map.removeLayer(marker));
        spawnedRecipientMarkers = [];

        const clusteredRecipients = [];

        recipients.forEach(recipient => {
            if(recipient.status == 'Confirmed') confirmedRecipients.push(recipient);
            //get recipient distance
            const userLatLng = L.latLng(userLocation.lat, userLocation.lng);
            const recipientLatLng = L.latLng(recipient.lat, recipient.lng);    
            //make to mile not km
            let distance = userLatLng.distanceTo(recipientLatLng) / 1000 * 0.621371;
            recipient.distance = distance.toFixed(1); // convert to string only when displaying
            if (distance < minDistance) {
                minDistance = distance;
            }

            let isClustered = false;

            for (let i = 0; i < clusteredRecipients.length; i++) {
                const clusteredRecipient = clusteredRecipients[i];
                const distance = haversineDistance(recipient.lat, recipient.lng, clusteredRecipient.lat, clusteredRecipient.lng);

                // Cluster recipients within 0.2 miles
                if (distance <= 0.05) {
                    clusteredRecipient.recipients.push(recipient);
                    isClustered = true;
                    break;
                }
            }

            if (!isClustered) {
                clusteredRecipients.push({
                    lat: recipient.lat,
                    lng: recipient.lng,
                    recipients: [recipient]
                });
            }
        });

        // Create new markers for clusters
        clusteredRecipients.forEach(cluster => {
            const [lng, lat] = [cluster.lng, cluster.lat];

            let marker;

            // Bind popup with recipient details
            let isMouseOver = false;
            // Create a div element for the
            const recipientDiv = document.createElement('div');
            recipientDiv.classList.add('recipient-marker-div');
            recipientDiv.style.display = 'none';
            
            
            // Create a div element for the distance
            // let recipientDistanceDiv = document.createElement('div');
            // Create a div element for the address
            // let recipientAddressDiv = document.createElement('div')



    // Loop through each recipient in the cluster
    cluster.recipients.forEach((recipient, index) => {
        // Create a container for each recipient's details
        const recipientDetailsDiv = document.createElement('div');
        recipientDetailsDiv.classList.add('recipient-details');

        const recipientData = document.createElement('div');
        const box = recipient.items > 1 ? `${recipient.items} Boxes` : `${recipient.items} Box`;
        // recipientData.innerText = `${recipient.address} | ${box}`;
        recipientData.innerText = `${recipient.address} | ${box} | ${recipient.distance} miles (${Math.ceil(recipient.distance*2+1)} minutes) away`;
        recipientData.style.marginBottom = '5px';
        recipientDetailsDiv.appendChild(recipientData);

        //add delete button
        const recipientDeleteBtn = document.createElement('button');
        recipientDeleteBtn.classList.add('general-button');
        recipientDeleteBtn.style.display = 'none';
        recipientDeleteBtn.style.backgroundColor = 'red';
        recipientDeleteBtn.innerText = translate('Deselect');
        recipientDeleteBtn.addEventListener('click', ()=>{
            if(confirm(`${translate('Are you sure you want to deselect')} ${recipient.name}?`))
                window.location.href = "php/select_recipient.php?recipient_id="+recipient.id+"&for=deselect";
        }); 

        // Add a "Select" button for each recipient
        const recipientSelectBtn = document.createElement('button');
        recipientSelectBtn.classList.add('general-button');
        recipientSelectBtn.innerText = translate('Select'); 
        recipientSelectBtn.addEventListener('click', () => {
            if (confirm(`${translate('Are you sure you want to select')} ${recipient.name}?`)) {
                window.location.href = `php/select_recipient.php?recipient_id=${recipient.id}&for=select`;
            }
        });

        // Append the button to the recipient details
        recipientDetailsDiv.appendChild(recipientSelectBtn);
        recipientDetailsDiv.appendChild(recipientDeleteBtn);

        // Append the recipient details to the main popup div
        recipientDiv.appendChild(recipientDetailsDiv);

        // Add a horizontal line between recipients
        if(cluster.recipients.length > 1 && (index < cluster.recipients.length - 1) ) recipientDiv.appendChild(document.createElement('hr'));

    function selectRecipient(){
            if(recipient.volunteer_id == volunteerID) {
                selectedRecipients.push(recipient);   
                totalItems += parseInt(recipient.items);
            }
            var greenIcon = new L.Icon({
                iconUrl: 'https://raw.githubusercontent.com/pointhi/leaflet-color-markers/master/img/marker-icon-2x-green.png',
                shadowUrl: 'https://cdnjs.cloudflare.com/ajax/libs/leaflet/0.7.7/images/marker-shadow.png',
                iconSize: [25, 41],
                iconAnchor: [12, 41],
                popupAnchor: [1, -34],
                shadowSize: [41, 41]
            });
            marker.remove();
            let newMarker = L.marker([lat,lng], {icon: greenIcon}).addTo(map);
            recipientSelectBtn.style.display = 'none';
            recipientDeleteBtn.style.display = 'block';
            let isMouseOverNew = false;
            // Add a mouseover event listener to the newMarker
            newMarker.on('mouseover', function (e) {
                isMouseOverNew = true;
                showRecipientDiv(e, recipientDiv);
            }); 
        }

        if (cluster.recipients.length > 1) {
            // Cluster marker
            marker = L.marker([lat, lng], {
                icon: L.divIcon({
                    html: `<div class="cluster-marker">${cluster.recipients.length}</div>`,
                    className: 'cluster',
                    iconSize: [30, 30]
                })
            });
            marker.addTo(map);
        }else{
            marker = L.marker([lat, lng]);
            marker.addTo(map);
        }


        const volunteerID = document.getElementById('volunteerID').value;
        if(recipient.volunteer_id == volunteerID) selectRecipient();
                



                
            marker.on('mouseover', (e) => {
                isMouseOver = true;
                showRecipientDiv(e, recipientDiv);
            });

            marker.on('mouseout', function () {
                isMouseOver = false;
                setTimeout(function () {
                    if (!isMouseOver) {
                        recipientDiv.style.display = 'none';
                    }
                }, 300);
            });
            // Add a mouseover event listener to the div
            recipientDiv.addEventListener('mouseover', function () {
                isMouseOver = true;
                document.querySelector('body').appendChild(style); //add the style that sets the scroll bar to shown always 
            });
            // Add a mouseout event listener to the div
            recipientDiv.addEventListener('mouseout', function () {
                isMouseOver = false;
                setTimeout(function () {
                    if (!isMouseOver) {
                        recipientDiv.style.display = 'none';
                        // document.querySelector('body').removeChild(style);
                    }
                }, 200);
            });
            
        });

            spawnedRecipientMarkers.push(marker);
        });
    }
    
    function showRecipientDiv(e, recipientDiv) {
        // Append the div to the page
        document.querySelector('body').appendChild(recipientDiv);
        recipientDiv.style.display = 'grid';
        recipientDiv.style.top = e.originalEvent.clientY + window.scrollY - recipientDiv.clientHeight+ "px";
        recipientDiv.style.left = e.originalEvent.clientX -recipientDiv.clientWidth/2 + "px";
    }


    // Update clusters initially and on map movements
    updateClusters();
    map.on('moveend', updateClusters);

    // Handle map center and zoom level changes
    map.on('moveend', function() {
        const center = map.getCenter();
        localStorage.setItem('mapCenterLat', center.lat);
        localStorage.setItem('mapCenterLng', center.lng);
    });

    map.on('zoomend', function() {
        const zoom = map.getZoom();
        localStorage.setItem('mapZoom', zoom);
    });
    
    let zoomLevel;
    if(minDistance < 0.5)      zoomLevel = 15;
    else if(minDistance < 1)   zoomLevel = 14;
    else if(minDistance < 5)   zoomLevel = 13;
    else if(minDistance < 10)  zoomLevel = 12;
    else if(minDistance < 20)  zoomLevel = 11;
    else if(minDistance < 50)  zoomLevel = 10;
    else if(minDistance < 100) zoomLevel = 9;
    else if(minDistance < 200) zoomLevel = 8.5;
    else if(minDistance < 500) zoomLevel = 8;
    else zoomLevel = 7;

    if (!zoom) map.setZoom(zoomLevel); // Initialize with default zoom level









    const summaryHeader = document.getElementById('selected-recipients-header');
    const collected = document.getElementById('collected').value;

    // ---------------------------- Collect Locations ----------------------------
    if(collected==0 && collectLocation){
        summaryHeader.innerHTML = `<h2 class="center-text">${translate('Collect Locations:')}</h2>`;
        selectedRecipients.sort((a, b) => a.distance - b.distance);

        //recreate it as a table
        const recipientsCollectTable = document.createElement('table');
        recipientsCollectTable.innerHTML = `
        <tr>
            <th>${translate('Starting Time')}</th>
            <th>${translate('Phone')}</th>
            <th>${translate('Address')}</th>
            <th>${translate('Maps')}</th>
        </tr>
        `;
        collectLocation.forEach(collectLocation => {
            const recipientRow = document.createElement('tr');
            recipientRow.innerHTML = `
            <td>${collectLocation.pickup_time}</td>
            <td><a href="tel:${collectLocation.phone}">${collectLocation.phone}</a></td>
            <td>${collectLocation.address}</td>
            <td class="center-text">
            <a href="https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(collectLocation.address)}" target="_blank">Google</a>
            <hr>
            <a href="http://maps.apple.com/?daddr=${encodeURIComponent(collectLocation.address)}" target="_blank">Apple</a>
            </td>  

            `;
            recipientsCollectTable.appendChild(recipientRow);
        });

        summaryHeader.appendChild(recipientsCollectTable);

        //confirm pickup button
        const pickupHeader = document.createElement('h4');
        pickupHeader.innerText = translate('If you arrived at the pickup location and picked up the')+' '+totalItems+' boxes'+translate(', please click the button below to confirm the pickup.');
        pickupHeader.style.textAlign = 'center';
        pickupHeader.style.margin = '0 0 3px 0';
        summaryHeader.appendChild(pickupHeader);
        
        const confirmPickupBtn = document.createElement('button');
        confirmPickupBtn.classList.add('general-button');
        confirmPickupBtn.style.backgroundColor = 'blue';
        confirmPickupBtn.style.width = '60%';
        confirmPickupBtn.style.marginLeft = '20%';
        confirmPickupBtn.style.marginRight = '20%';
        confirmPickupBtn.style.marginBottom = '4px';
        confirmPickupBtn.style.color = 'white';
        
        confirmPickupBtn.innerText = translate('Confirm Pickup');
        confirmPickupBtn.addEventListener('click', ()=>{
            if(confirm('Are you sure you want to confirm the pickup of the selected recipients?'))
            window.location.href = 'php/select_recipient.php?for=pickup';
        });
        summaryHeader.appendChild(confirmPickupBtn);
        summaryHeader.appendChild(document.createElement('hr'));
    }


// ---------------------------- Recipients Table ----------------------------

    
    const recipientsTableDiv = document.getElementById('recipients-table');
    
    const selectedRecipientsCount = document.getElementById('selected-recipients-count');
    selectedRecipientsCount.innerText =  ''+selectedRecipients.length;
    
    const totalItemsDiv = document.createElement('div');
    totalItemsDiv.classList.add('center-text');
    totalItemsDiv.innerHTML = `
    <h2 style="color:red; margin: 5px !important;">${translate('Total Required Boxes:')} ${totalItems}</h2>
     <h4 style="margin:0">${translate('Please confirm the delivery after dropping off each box to it\'s recipient.')}</h4>    `;
    recipientsTableDiv.appendChild(totalItemsDiv);

    const recipientsTable = document.createElement('table');

    if(confirmedRecipients.length > 0){
        actionBtnHead = `<th>${translate('Actions')}</th>`
        console.log(selectedRecipients.length)
        if(confirmedRecipients.length == selectedRecipients.length) actionBtnHead = ``
        if(!collected) actionBtnHead = ``
        
        recipientsTable.innerHTML = `
        <tr>
            <th>${translate('Recipient')}</th>
            <th>${translate('Phone')}</th>
            <th>${translate('Items')}</th>
            <th>${translate('Address')}</th>
            <th>${translate('Maps')}</th>
            ${actionBtnHead}
        </tr>
        `;
        selectedRecipients.forEach(recipient => {
            const recipientRow = document.createElement('tr');
            let complexName = "";
            let gateCode = "";
            let apt_num = "";
            if(recipient.comp_name) complexName = ('Complex Name:') + (recipient.comp_name == null ? '' : recipient.comp_name+'<hr>');
            if(recipient.gate_code) gateCode =  ('Gate Code:') +( recipient.gate_code == null ? '' : recipient.gate_code+'<hr>');
            if(recipient.apt_num) apt_num =  ('Apt:') +( recipient.apt_num == null ? '' : recipient.apt_num+'<hr>');
            
            let actionBtn = "";
            if(collected & recipient.status == 'Confirmed'){
                actionBtn = `<td><button style="font-size:9px; background-color:blue; color:white" class="general-button" onclick="if(confirm('${translate('Are you sure you want to confirm this recipient as Delivered?')}')) window.location.href='php/select_recipient.php?recipient_id=${recipient.id}&for=completed'">${translate('Confirm Delivery')}</button></td>`;
            } 
            else if(recipient.status != 'Confirmed'){
                actionBtn=`<td><button style="background-color:green;" class="general-button" onclick="if(confirm('${translate('Are you sure you want to confirm this recipient?')}')) window.location.href='php/select_recipient.php?recipient_id=${recipient.id}&for=confirm'">${translate('Confirm')}</button></td>`
            } 

            recipientRow.innerHTML = `
            <td>${recipient.name}</td>
            <td><a href="tel:${recipient.phone}">${recipient.phone}</a></td>
            <td>${recipient.items}</td>
            <td class="center-text">
            ${complexName}
            ${gateCode}
            ${apt_num}
            ${recipient.address}, ${recipient.city}
            <br>
            ${recipient.hotel_info == null ? '' : 'Hotel:'+recipient.hotel_info}
            <td class="center-text">
            <a href="https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(recipient.address + ', ' + recipient.city)}" target="_blank">Google</a>
            <hr>
            <a href="http://maps.apple.com/?daddr=${encodeURIComponent(recipient.address + ', ' + recipient.city)}" target="_blank">Apple</a>
            </td>
            ` + actionBtn;


            recipientsTable.appendChild(recipientRow);
        });
    }
    else{
        recipientsTable.innerHTML = `
        <tr>
            <th>${translate('Items')}</th>
            <th>${translate('Address')}</th>
            <th>${translate('Maps')}</th>
        </tr>
        `;
        selectedRecipients.forEach(recipient => {
            const recipientRow = document.createElement('tr');
            recipientRow.innerHTML = `
            <td>${recipient.items}</td>
            <td>${recipient.address} , ${recipient.city}</td>
            <td class="center-text">
            <a href="https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(recipient.address + ', ' + recipient.city)}" target="_blank">Google</a>
            <hr>
            <a href="http://maps.apple.com/?daddr=${encodeURIComponent(recipient.address + ', ' + recipient.city)}" target="_blank">Apple</a>
            </td>
            `;
            recipientsTable.appendChild(recipientRow);
        });
    }
    recipientsTableDiv.appendChild(recipientsTable);
    
    const condirmBtn = document.createElement('button');
    condirmBtn.classList.add('general-button');
    if(selectedRecipients.length == 0){
        const paragraph = document.createElement('h1');
        paragraph.style.color = 'red';
        paragraph.innerText = translate('Please Select recipients from the map');
        recipientsTableDiv.appendChild(paragraph);
    }
    else{
        if(selectedRecipients.length == confirmedRecipients.length){
            showSummary();
            condirmBtn.style.backgroundColor = 'green';
            condirmBtn.style.color = 'white';
            condirmBtn.innerText = translate('Open Full Route On Google Maps');
            // arrange the recipients based on their distance from the user
            condirmBtn.addEventListener('click', function(){
                let confirmTXT = translate('You will be redirected to Google Maps to view the route of the selected recipients.');
                if(selectedRecipients.length >10) confirmTXT += ' '+translate('It will cointain a maximum of 10 recipient waypoints, who are the nearest recipients to you. So please confirm delivery then re-click the button to view the route.');
                if (confirm(confirmTXT)) {
                    // Adjust the number of waypoints based on the device
                    const isMobile = /Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent);
                    const maxWaypoints = isMobile ? 10 : 15; // Fewer waypoints for mobile devices
                    const limitedRecipients = confirmedRecipients.slice(0, maxWaypoints);
            
                    // Encode recipient locations
                    let recipientLocations = limitedRecipients.map(confirmedRecipient =>
                        encodeURIComponent(`${confirmedRecipient.address}, ${confirmedRecipient.city}`)
                    );
            
                    // Join waypoints with "|"
                    let recipientLocationsString = recipientLocations.join('|');
            
                    // Generate Google Maps link with optimization
                    let googleMapsLink = `https://www.google.com/maps/dir/?api=1&waypoints=${recipientLocationsString}&optimize=true`;
            
                    // Open the link in a new tab
                    window.open(googleMapsLink);
                }
            });
            const deleteAllBtn = document.createElement('button');
            deleteAllBtn.classList.add('general-button');
            deleteAllBtn.style.backgroundColor = 'red';
            deleteAllBtn.style.color = 'white';
            deleteAllBtn.style.marginBottom = '5px';
            deleteAllBtn.innerText = translate('Deselect All'); 
            deleteAllBtn.addEventListener('click', function(){
                if(confirm(translate('Are you sure you want to delete all recipients?'))){
                    window.location.href = 'php/select_recipient.php?for=deleteAll&recipients='+selectedRecipients.map(recipient => recipient.id).join(',');
                }
            });
            recipientsTableDiv.appendChild(deleteAllBtn);
        }else{
            setTimeout(() => {
                if(selectedRecipients.length >0){
                    alert(translate('Please Don\'t forget to confirm the recipients after selecting all of them from the bottom of the page'));
                }
            }, 1000);

            condirmBtn.innerText = 'Confirm All Recipients';
            condirmBtn.addEventListener('click', function(){
                if(confirm(translate('Are you sure you want to confirm these recipients?'))){
                    window.location.href = 'php/select_recipient.php?for=confirmAll&recipients='+selectedRecipients.map(recipient => recipient.id).join(',');
                }
            });
        }
        recipientsTableDiv.appendChild(condirmBtn);
    }




}









// locattion of the recipient


// let userLocation






document.addEventListener('DOMContentLoaded', ()=>{
    //fetch recipients
    fetch('php/fetch_recipients.php')
        .then(response => response.json())
        .then(data => {
            initMap(data.recipients, data.collectLocation);
        });

    //locate me
    const locateBtn = document.getElementById('locate-button-div');
    locateBtn.addEventListener('click',locateMe);

    //location input
    var locationInput = document.querySelector('.search-bar');    
    locationInput.addEventListener('change', function () {
        // Get the user's input
        var userInput = locationInput.value;
        // Send a request to the OpenCage geocoding API
        const apiKey = 'OPEN_CAGE_API_KEY'; // Replace with your OpenCage API key
        fetch(`https://api.opencagedata.com/geocode/v1/json?q=${userInput}&key=${apiKey}`)
            .then(response => response.json())
            .then(async data => {
                if (data.results.length > 0) {
                    // Use the latitude and longitude of the first result
                    var lat = data.results[0].geometry.lat;
                    var lng = data.results[0].geometry.lng;        
                    sessionStorage.setItem('userLocation', JSON.stringify({ type: 'coords', value: { lat: lat, lng: lng } }));
                    //delete local storage
                    localStorage.removeItem('mapCenterLat');
                    localStorage.removeItem('mapCenterLng');
                    localStorage.removeItem('mapZoom');

                    //log the location
                    const volunteerID = document.getElementById('volunteerID').value;
                    if(volunteerID){
                        const formData = new FormData();
                        formData.append('volunteer_id', volunteerID);
                        formData.append('address', userInput);
                        formData.append('latitude', lat);
                        formData.append('longitude', lng);
                        formData.append('method', 'Manually');
                        const response = await fetch('php/location_logs.php', {
                            method: 'POST',
                            body: formData
                        });
                    }

                    //refresh the page
                    location.reload();
                }else{
                    alert('No results found');
                }
            });
    });
});

function locateMe() {
    if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(showPosition, locateError);
    }
    async function showPosition(position) {
        const latitude = position.coords.latitude;
        const longitude = position.coords.longitude;
        sessionStorage.setItem('userLocation', JSON.stringify({ type: 'coords', value: { lat: latitude, lng: longitude } }));
        //delete local storage
        localStorage.removeItem('mapCenterLat');
        localStorage.removeItem('mapCenterLng');
        localStorage.removeItem('mapZoom');
        
        //referse geocoding
        const response = await fetch(`https://nominatim.openstreetmap.org/reverse?format=json&lat=${latitude}&lon=${longitude}`);
        const data = await response.json();
        const address = data.display_name;

        //log the location
        const volunteerID = document.getElementById('volunteerID').value;
        if(volunteerID){
            const formData = new FormData();
            formData.append('volunteer_id', volunteerID);
            formData.append('address', address);
            formData.append('latitude', latitude);
            formData.append('longitude', longitude);
            formData.append('method', 'Locate me');
            const response = await fetch('php/location_logs.php', {
                method: 'POST',
                body: formData
            });
        }

        location.reload();

    }

    function locateError(error) {
        const locateErrorMsg = document.getElementById('locate-me-error');
        locateErrorMsg.style.display = 'block';
        switch (error.code) {
            case error.PERMISSION_DENIED:
                locateErrorMsg.style.display = 'block';
                locateErrorMsg.innerHTML = "Denied access for your location."
                break;
            case error.POSITION_UNAVAILABLE:
                // Handle the error
                break;
        }
    }
}


//bottom button
const summary = document.getElementById('summary');
const arrowUp = document.querySelector('.fa-arrow-up');
const arrowDown = document.querySelector('.fa-arrow-down');

const style = document.createElement('style');
style.innerHTML = `
::-webkit-scrollbar {
    -webkit-appearance: none;
    width: 7px;
    }
    ::-webkit-scrollbar-thumb {
    border-radius: 4px;
    background-color: rgba(0, 0, 0, .5);
    -webkit-box-shadow: 0 0 1px rgba(255, 255, 255, .5);
    }
    `;



arrowUp.onclick = showSummary;
arrowDown.onclick = hideSummary;


function hideSummary(){
    summary.style.height = '50px';
    summary.style.transition = 'all 0.5s';
    arrowDown.style.display = 'none';
    arrowUp.style.display = 'block';
    document.querySelector('body').removeChild(style);
}
function showSummary(){
    summary.style.height = '70vh';
    summary.style.transition = 'all 0.5s';
    arrowUp.style.display = 'none';
    arrowDown.style.display = 'block';
    document.querySelector('body').appendChild(style);
}
